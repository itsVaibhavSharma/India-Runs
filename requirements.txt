# Core ML / numeric
numpy>=1.26.0,<2.0
scikit-learn>=1.4.0,<2.0

# Sentence Transformers (CPU-only torch bundled)
# Using CPU-only torch to keep image size small
torch==2.2.2+cpu --index-url https://download.pytorch.org/whl/cpu
sentence-transformers==3.0.1
transformers>=4.40.0
tokenizers>=0.19.0
huggingface-hub>=0.23.0

# Data
pandas>=2.1.0

# Progress & utilities
tqdm>=4.66.0

# File formats
python-docx>=1.1.0
PyYAML>=6.0.1

# Streamlit (for the demo app)
streamlit>=1.30.0
plotly>=5.20.0
altair>=5.0.0

# Fast JSON (optional but speeds up JSONL parsing)
ujson>=5.9.0; platform_system != "Windows" or python_version >= "3.11"