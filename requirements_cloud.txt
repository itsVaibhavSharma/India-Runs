# ============================================================
# Streamlit Cloud requirements — CPU-friendly no-GPU version
# Note: torch is installed separately from PyTorch's CPU-only index
# ============================================================

# Core numeric / ML
numpy>=1.26.0,<2.0
scikit-learn>=1.4.0,<2.0

# Sentence Transformers (will pull in torch automatically on cloud)
sentence-transformers==3.0.1
transformers>=4.40.0,<5.0
tokenizers>=0.19.0
huggingface-hub>=0.23.0

# Data processing
pandas>=2.1.0
tqdm>=4.66.0

# File formats
python-docx>=1.1.0
PyYAML>=6.0.1

# Streamlit demo / deployment
streamlit>=1.35.0
plotly>=5.20.0
altair>=5.0.0
